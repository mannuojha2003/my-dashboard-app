{
  "name": "my-dashboard-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && node node_modules/vite/bin/vite.js build --outDir ../backend/dist/public && cd ../backend && npm install && npm run build",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
